name: Build and deploy JAR to Azure Web App - trustworthy-product-reviews-group24

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: microsoft
          java-version: '17'

      - name: Build JAR with Maven
        run: mvn -B -DskipTests clean package

      - name: Show build outputs (debug)
        run: dir target

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: jar-artifact           
          path: target/*.jar           
          if-no-files-found: error

  deploy:
    runs-on: windows-latest
    needs: build
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download JAR artifact
        uses: actions/download-artifact@v4
        with:
          name: jar-artifact       

      - name: List downloaded files (debug)
        run: dir

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_2F6C13D330014183B9DE44DAB15E174F }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_C94CEB95511745B68A4B627BC1F28F2A }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_6A06DB09B6DC445681B39311276A12FE }}

      - name: Deploy to Azure Web App (Java SE)
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'trustworthy-product-reviews-group24'
          slot-name: 'Production'
          package: 'app.jar'        
